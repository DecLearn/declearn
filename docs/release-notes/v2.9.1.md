# declearn v2.6.1

Released: 26/08/2026

This is a subminor release that fixes:
- the documentation generation for DecLearn v.2.9.x
- the CI/CD config in `gitlab-ci.yml`: the fix should allow to trigger
  the `benchmark` job in the pipeline when creating and pushing a release tag
  (ex: `v2.9.1`)